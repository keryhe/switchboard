export const environment = {
  production: false,
  // API base URL; SignalR negotiates through here. No proxy service URL needed — Angular
  // never talks to it directly (see docs/docs/08-sample-app.md § Configuration Summary).
  apiUrl: '',
};
